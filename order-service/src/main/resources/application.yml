server:
  port: 8083

spring:
  application:
    name: order-service

kafka:
  bootstrap-servers: kafka:9092
  producer:
    acks: all
    enable-idempotence: true

sentinel:
  signals-topic: signals.raw

order:
  emit-interval-ms: 5000
server:
  port: 8084

spring:
  application:
    name: inventory-service

kafka:
  bootstrap-servers: kafka:9092
  producer:
    acks: all
    enable-idempotence: true

sentinel:
  signals-topic: signals.raw

inventory:
  emit-interval-ms: 5000